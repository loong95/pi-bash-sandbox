/**
 * pi-bash-sandbox extension entry point.
 *
 * Phase 0 ships the pure core only (config, project resolution, bwrap argv
 * construction). Phase 1 registers the sandboxed `bash` tool, the `user_bash`
 * handler, and the `/sandbox` command here.
 */

export default function piBashSandbox(): void {
	// Phase 1: register the sandboxed bash tool here.
}
